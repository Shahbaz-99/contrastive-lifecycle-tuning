# contrastive-lifecycle-tuning
A small sentiment analysis project showing a real problem in ML — models trained on "clean" text fail on real-world slang, sarcasm, and emojis. This repo trains a baseline model to show that failure, then fixes it using contrastive loss during a second training round, and lets you compare both in a simple two-tab UI.
